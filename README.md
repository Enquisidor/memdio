# memdio
A simple app that keeps a buffer of audio recording 24/7, an hour long or time set by user that overwrites &amp; truncates itself. v2 includes an audio editor for exporting clips instead of the whole buffer.
